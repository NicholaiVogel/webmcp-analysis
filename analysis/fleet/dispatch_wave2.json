[{"goal": "Blind review r1-0001 (repair)", "context": "You are reviewer r1-0001 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0001.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0001.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0011 (repair)", "context": "You are reviewer r1-0011 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0011.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0011.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0013 (repair)", "context": "You are reviewer r1-0013 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0013.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0013.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0014 (repair)", "context": "You are reviewer r1-0014 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0014.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0014.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0020 (repair)", "context": "You are reviewer r1-0020 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0020.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0020.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0028 (repair)", "context": "You are reviewer r1-0028 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0028.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0028.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0038 (repair)", "context": "You are reviewer r1-0038 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0038.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0038.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0043 (repair)", "context": "You are reviewer r1-0043 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0043.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0043.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0045 (repair)", "context": "You are reviewer r1-0045 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0045.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0045.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0050 (repair)", "context": "You are reviewer r1-0050 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0050.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0050.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0051 (repair)", "context": "You are reviewer r1-0051 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0051.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0051.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0055 (repair)", "context": "You are reviewer r1-0055 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0055.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0055.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0057 (repair)", "context": "You are reviewer r1-0057 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0057.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0057.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0058 (repair)", "context": "You are reviewer r1-0058 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0058.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0058.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0066 (repair)", "context": "You are reviewer r1-0066 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0066.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0066.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0067 (repair)", "context": "You are reviewer r1-0067 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0067.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0067.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0093 (repair)", "context": "You are reviewer r1-0093 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0093.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0093.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0095 (repair)", "context": "You are reviewer r1-0095 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0095.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0095.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0097 (repair)", "context": "You are reviewer r1-0097 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0097.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0097.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0098 (repair)", "context": "You are reviewer r1-0098 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0098.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0098.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0017 (repair)", "context": "You are reviewer r1-0017 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0017.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0017.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0059 (repair)", "context": "You are reviewer r1-0059 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0059.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0059.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0074 (repair)", "context": "You are reviewer r1-0074 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0074.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0074.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0029 (repair)", "context": "You are reviewer r1-0029 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0029.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0029.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0044 (repair)", "context": "You are reviewer r1-0044 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0044.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0044.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind review r1-0089 (repair)", "context": "You are reviewer r1-0089 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0089.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ - VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/r1-0089.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up. Read nothing else in the repo.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0100", "context": "You are reviewer r1-0100 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0100.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0100.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0101", "context": "You are reviewer r1-0101 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0101.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0101.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0102", "context": "You are reviewer r1-0102 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0102.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0102.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0103", "context": "You are reviewer r1-0103 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0103.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0103.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0104", "context": "You are reviewer r1-0104 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0104.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 1 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0104.jsonl. The output file MUST contain exactly 1 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0105", "context": "You are reviewer r1-0105 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0105.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0105.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0106", "context": "You are reviewer r1-0106 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0106.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0106.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0107", "context": "You are reviewer r1-0107 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0107.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0107.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0108", "context": "You are reviewer r1-0108 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0108.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0108.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0109", "context": "You are reviewer r1-0109 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0109.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0109.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0110", "context": "You are reviewer r1-0110 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0110.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0110.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0111", "context": "You are reviewer r1-0111 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0111.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0111.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0112", "context": "You are reviewer r1-0112 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0112.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0112.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0113", "context": "You are reviewer r1-0113 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0113.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0113.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0114", "context": "You are reviewer r1-0114 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0114.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0114.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0115", "context": "You are reviewer r1-0115 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0115.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0115.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0116", "context": "You are reviewer r1-0116 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0116.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0116.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0117", "context": "You are reviewer r1-0117 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0117.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0117.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0118", "context": "You are reviewer r1-0118 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0118.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0118.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0119", "context": "You are reviewer r1-0119 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0119.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 2 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0119.jsonl. The output file MUST contain exactly 2 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0120", "context": "You are reviewer r1-0120 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0120.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0120.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0121", "context": "You are reviewer r1-0121 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0121.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0121.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0122", "context": "You are reviewer r1-0122 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0122.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0122.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}, {"goal": "Blind Stage-1 review: r1-0123", "context": "You are reviewer r1-0123 in the WebMCP Challenge independent study. Read the file /mnt/work/webmcp-analysis/analysis/fleet/prompts/r1-0123.txt IN FULL and follow it exactly. It contains your role rules, the frozen rubric, the frozen category taxonomy, and 10 project packets. The packets reference image files (video frame sheets and Devpost page screenshots) relative to /mnt/work/webmcp-analysis/ \u2014 VIEW each referenced image directly (native vision) before scoring that project. Then write your judgments as newline-delimited JSON (one object per project, exact schema from the prompt) to /mnt/work/webmcp-analysis/analysis/results/analysis/results/r1-0123.jsonl. The output file MUST contain exactly 10 JSON lines, one per packet slug. Read nothing else in the repo \u2014 no other prompts, no CALIBRATION.md, no raw/hanclinto, no other results. If you hit a rate limit or transient error, wait 60-120 seconds and retry within your time budget rather than giving up.", "output_schema": {"type": "object", "properties": {"reviewer": {"type": "string"}, "written": {"type": "integer"}, "path": {"type": "string"}}, "required": ["reviewer", "written", "path"]}}]